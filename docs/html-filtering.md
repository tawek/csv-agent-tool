# Problem

The user wants to filter out spaces and line breaks from the exported HTML columns.

# Solution

The configuration dialog has a checkbox to enable this behavior per column.

When the checkbox is checked when exporting the csv file, the column that has the checkbox checked will be exported in a way that removes consecutive spaces and line breaks and replaces them with a single space.

