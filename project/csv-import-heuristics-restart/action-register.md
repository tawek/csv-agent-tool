# Action Register

| ID | Source | Finding | Disposition | Owner | Target | Status |
| --- | --- | --- | --- | --- | --- | --- |
| AR-1 | Leader | Existing `csv-auto-detect` work appeared misaligned with the newly stated intent to prefer project settings and needed to be discarded before restart. | fix now | Leader | this task | closed |
| AR-2 | Architect | The current flat `CsvConfig` model cannot cleanly represent separate import settings, separate export settings, and the "export settings not yet established before first import" state required by the clarified behavior. | fix now | Product Developer | this implementation | closed |
| AR-3 | Architect | The previous architecture draft required a normal-path import-review dialog, but the clarified intent rejects that burden. Any import-settings review UI must be limited to exception-path recovery after auto-detection failure. | reject | Architect | this spec/architecture revision | closed |
| AR-4 | Architect | `ConfigStore.save()` currently persists provider and generation settings but not app-level CSV settings, which weakens the fallback/default story and should be reconciled during implementation planning for the split import/export model. | fix now | Product Developer | follow-up remediation (closed via AR-9) | closed |
| AR-5 | Architect | `MainWindow`, `ProjectRepository`, and `CsvRepository` currently assume one shared CSV settings object for both read and write paths. Implementation must update those call sites consistently so open/import uses import settings while save/export uses export settings. | fix now | Product Developer | this implementation | closed |
| AR-6 | Architect | The revised import contract is not actually implemented: `MainWindow.load_csv()` passes the existing `project.csv.import_settings` directly into `CsvRepository.load()`, `CsvRepository` has no detection/sniffing helper, and the code then labels the resulting configured dialect as "detected". This leaves normal-path import dependent on prior/default settings instead of file heuristics and violates Use Case 5 plus the architecture's repository/workflow boundary. | fix now | Product Developer | follow-up remediation | closed |
| AR-7 | Architect | Import-setting persistence is incomplete. The revised spec requires effective import parsing settings for `encoding`, `delimiter`, `quotechar`, and `newline` to become the project's import settings, and first-import seeding must copy the detected parsing settings into export settings once. The implementation only updates/seeds `delimiter` and `quotechar`, so persisted import/export state can diverge from the actual imported file characteristics. | fix now | Product Developer | follow-up remediation | closed |
| AR-8 | Architect | Manual export settings entered through **Settings** before the first import are not treated as established. `SettingsDialog.get_config()` preserves `export_settings_initialized=False`, and `MainWindow.open_settings()` updates only `export_settings`, so the next first import silently reseeds and overwrites the user-chosen export parsing settings. This violates Use Case 2/5 and the architecture rule that explicit project settings are authoritative for writes once chosen. | fix now | Product Developer | follow-up remediation | closed |
| AR-9 | Architect | The review gate still fails because app-level CSV persistence remains contract-inconsistent in a behavior-affecting way. `MainWindow.open_settings()` updates `self.config.csv.export_settings` and calls `ConfigStore.save(self.config)`, but `ConfigStore.save()` drops `config.csv` entirely. On the next app launch `AppConfig.from_dict()` rebuilds `csv` from `{}`, which `CsvConfig.from_dict()` treats as legacy flat data and marks `export_settings_initialized=True`. This means confirmed CSV settings do not persist across restart and a fresh project after restart no longer satisfies the Use Case 2 invariant that export settings are not established until first import or explicit configuration. | fix now | Product Developer | follow-up remediation | closed |
| AR-10 | Architect | The AR-9 remediation still leaves the app-level CSV initialization contract inconsistent after **Settings** confirmation. `MainWindow.open_settings()` copies `updated.csv.export_settings` into `self.config.csv.export_settings` but never copies `updated.csv.export_settings_initialized` into `self.config.csv`. `ConfigStore.save(self.config)` therefore persists the old initialization flag (commonly `False`) even when the user explicitly established export settings. After restart, a fresh project can inherit the edited export values but still behave as "not yet established," allowing first import reseeding to overwrite explicit app-level settings. This remains a behavior-affecting persistence bug against Use Case 1/2/5. | fix now | Product Developer | follow-up remediation | closed |
| AR-11 | Architect | Final confirmation review found one remaining CSV-settings contract mismatch in the scoped implementation area. `SettingsDialog.get_config()` treats an empty delimiter field as `","` (`_single_char_value(..., default=",")`) instead of the specified fresh/default CSV delimiter `";"`. A user who clears the delimiter field and confirms **Settings** silently establishes comma-separated export settings, which conflicts with Use Case 1's CSV default contract and with the split-config persistence model now being reviewed. | fix now | Product Developer | this implementation | closed |
