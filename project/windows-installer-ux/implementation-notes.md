# Windows Installer UX

- User feedback: `install.bat` is too silent and appears stalled during large copies.
- Requested behavior: the Windows install script should clearly show progress/activity so users can tell installation is still running.
- Requested follow-up: upload the improved distribution to Google Drive.
- Constraint: keep the installation entry point as a simple `install.bat` shipped alongside the packaged app.
