# Status

- Current phase: completed.
- Completed work: replaced the silent installer copy flow with a visible runtime-selected copy path that uses `robocopy` when available and falls back to visible `xcopy` otherwise.
- Publishing: updated `install.bat` in the remote Windows dist folder and uploaded the revised script directly to Google Drive.
