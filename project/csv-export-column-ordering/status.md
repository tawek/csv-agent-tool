# CSV Export Column Ordering Status

## Current State

- Feature implemented
- Regression tests added
- Architect review gate passed
- Action register remains open for deferred and monitor items

## Open Follow-ups

- AR-2 — config ownership/persistence boundary cleanup
- AR-3 — export-dialog naming/contract cleanup
- AR-4 — monitor persisted `export_order` canonicalization need
- QA-2 — monitor stronger integration coverage need
- QA-3 — monitor explicit `null` handling asymmetry

## Closure Rule

This feature stream is functionally complete, but the workspace stays active until deferred/monitor items are either resolved, superseded, or explicitly retired.
